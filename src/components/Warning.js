import React from 'react';
import PropTypes from 'prop-types';

const Warning = () => (
  <div className='small-bummer'>
      <h3>Sorry! 😭</h3>
      <p>This game requires hover, so there is no mobile version.</p>
  </div>
);

export default Warning;
